import React from "react";

import { Link } from "react-router-dom";

export default function FindPeople() {
    return (
        <div>
            <h1> i m the app </h1>
        </div>
    );
}
